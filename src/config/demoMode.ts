/**
 * Demo Mode Configuration
 * Set to true to use localStorage instead of Firebase
 * This allows testing without Firebase setup
 *
 * For this Glacier Sentinel demo, we default to TRUE so you can run
 * the full app (auth, dashboards, alerts) without configuring Firebase.
 */
export const DEMO_MODE = false; // Set to false when Firebase is configured

